// import { APIGatewayEvent, Context } from 'aws-lambda';
// import { handler } from '../../handlers/createAuction';

describe('createAuction handler', () => {
  test('correctly create auction', async () => {
    // const event = {
    //   title: 'Test Auction',
    //   requestContext: { authorizer: { email: 'someemail@test.test' } },
    // } as APIGatewayEvent;

    // const response = await handler.createAuction(event);

    // expect(response).toStrictEqual({
    //   statusCode: 201,
    //   body: JSON.stringify(newAuction),
    // });
  });
});
